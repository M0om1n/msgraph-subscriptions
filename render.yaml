services:
  - type: web
    name: hello-msgraph-subscriptions
    runtime: node
    plan: free
    buildCommand: yarn install --frozen-lockfile
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
